/// Interactive Books — core library containing all business logic.
///
/// This library implements the full RAG pipeline: ingestion, embedding,
/// retrieval, and agentic chat. It is shared between the CLI and the
/// SwiftUI app.
public enum InteractiveBooksCore {
    public static let version = "0.1.0"
}
